{
  "name": "@planetx/database",
  "version": "0.0.0",
  "scripts": {
    "build": "tsc",
    "migrate": "sequelize-cli db:migrate",
    "seed": "sequelize-cli db:seed:all",
    "undo-migration": "sequelize-cli db:migrate:undo",
    "lint": "eslint \"**/*.ts*\"",
    "lint:fix": "eslint src --fix --ext .ts --cache"
  },
  "devDependencies": {
    "@planetx/eslint-config": "*",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "mysql2": "^3.2.4",
    "sequelize": "^6.32.1",
    "sequelize-cli": "^6.6.0"
  }
}
